import { Story, Meta } from '@storybook/react/types-6-0'
import FinishedModal from '.'

export default {
  title: 'FinishedModal',
  component: FinishedModal
} as Meta

export const Default: Story = () => <FinishedModal />
